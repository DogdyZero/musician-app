import { MemoryCupomDataBase } from './memory-cupom-data-base';

describe('MemoryCupomDataBase', () => {
  it('should create an instance', () => {
    expect(new MemoryCupomDataBase()).toBeTruthy();
  });
});
